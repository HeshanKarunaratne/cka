apiVersion: v1
kind: PersistentVolume
metadata:
  name: persistent-volume
spec:
  accessModes:
    - ReadWriteOnce
    # - ReadWriteMany
    # - ReadOnlyMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /tmp/data