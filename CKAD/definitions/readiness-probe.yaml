apiVersion: v1
kind: Pod
metadata:
  name: readiness-probe
  labels:
    app: myapp
spec:
  containers:
    - name: nginx
      image: nginx:1.12
      ports:
        - containerPort: 80
      readinessProbe:
        httpGet:
          path: /api/ready
          port: 80